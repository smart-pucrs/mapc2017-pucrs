{begin namespace (taDefinitions, global)}  

agentsBid(28).

//taUtilityGoal("maximize").
taUtilityGoal("minimize").

maxUtility(50).

typeMin(sd,"N").
typeMax(sd,"N").

typeMin(tcn,1).
typeMax(tcn,1).

typeMin(tcl,1).
typeMax(tcl,"N").

{end}